README
======

This is a simple README file for the html5-compass-navigation project.

HTML5 compass navigation is a simple attempt to create a nice navigation system using HTML5 technologies.
It's called compass due to its main component that looks like a compass (or so).